import React from 'react'

/**
 * @description Тип данных, возвращаемых при рендере.
 *  Описывает все варианты данных, которые могут быть возвращены при рендере.
 */
type View = string | React.ReactNode | React.ReactNodeArray

export default View
